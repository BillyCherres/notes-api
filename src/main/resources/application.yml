server:
  port: 8080

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-battoy4bsiqoq11t.us.auth0.com/

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

auth0:
  audience: https://notes-api